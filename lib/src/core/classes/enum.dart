
enum Status {idle,loading,success,failed,empty,started,error}
enum AuthStatus {loggedOut,loggedIn}
enum CameraStatus {hasImage,ready,idle,denied}
enum Language {indonesian,english}
enum ConversationStatus {ex,ew,cr,en,vu,nt,lc}
enum boxType {history,speciesList,general}
